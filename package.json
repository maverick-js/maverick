{
  "name": "maverick-workspace",
  "version": "0.43.2",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --if-present test",
    "test:client": "pnpm -r --if-present test:client",
    "test:client:watch": "pnpm -r --if-present test:client:watch",
    "test:server": "pnpm -r --if-present test:server",
    "test:server:watch": "pnpm -r --if-present test:server:watch",
    "format": "vp fmt",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s",
    "release": "pnpm build && node ./.scripts/release.js"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "conventional-changelog-cli": "^5.0.0",
    "enquirer": "^2.4.1",
    "execa": "^9.6.0",
    "kleur": "^4.1.5",
    "minimist": "^1.2.8",
    "semver": "^7.8.0",
    "simple-git-hooks": "^2.13.0",
    "vite-plus": "catalog:",
    "vitest": "catalog:"
  },
  "simple-git-hooks": {
    "pre-commit": "vp staged"
  },
  "engines": {
    "node": ">=22",
    "pnpm": ">=10"
  },
  "packageManager": "pnpm@10.12.1"
}
